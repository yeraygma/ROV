# t0= 2018-06-21 16:39:54

#establece la fecha y hora inicial a la que comenzo a grabar el ROV en formato POSIXct. 
t0 <- as.POSIXct("2018-06-21 16:39:54", format = "%Y-%m-%d %H:%M:%S")
t0
data$t <- 
t0s <-cat(t0,"\n")


as.POSIXct(t0s, origin = "1970-01-01 00:00:00")

#convierte un tiempo en segundos timestamp (desde1970) a formato en tiempo
as.POSIXct(1529595594, origin = "1970-01-01 00:00:00")